import { Property } from 'csstype';

export type TopTypes = Property.Top;

export interface TopStyleProps {
  top?: TopTypes;
  topXs?: TopTypes;
  topSm?: TopTypes;
  topMd?: TopTypes;
  topLg?: TopTypes;
  topXl?: TopTypes;
  topXxl?: TopTypes;
}
